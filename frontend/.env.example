# Default backend URL. Override per environment as needed.
# - Local dev (FastAPI on port 8000): http://localhost:8000
# - Docker/prod: use the service name or public DNS
VITE_API_URL=http://localhost:8000
